{
  "version": 2,
  "build": {
    "env": {
      "NODE_ENV": "production"
    },
    "command": "npm run build",
    "outputDirectory": "dist"
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}